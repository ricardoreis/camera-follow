# Visão computacional (Fases 1-5)
opencv-python==4.13.0
numpy==2.4.6

# Controle do braço B601-dm / motores Damiao (Fase 6+)
# O pacote de controle em si (reBotArm_control_py) NÃO é instalado por pip:
# importamos via sys.path apontando para ~/GITHUB/reBotArm_control_py
motorbridge>=0.1.7   # ponte serial<->CAN (MotorBridge em /dev/ttyACM0)
pyyaml>=6.0          # lê o config/arm.yaml
pin>=3.9.0           # pinocchio (cinemática); puxado pelo __init__ do pacote

# Painel web (seguir_ik_web.py) — servidor FastAPI + websocket + vídeo MJPEG.
# O front (React+Tailwind) fica em web/ e precisa de Node/npm: npm install && npm run build
fastapi>=0.110            # servidor web (rotas /video, /ws, serve o build do React)
uvicorn[standard]>=0.29   # ASGI server (roda o FastAPI numa thread daemon)

# Percepção avançada (PLANO_CRIATURA: corpo/mãos/identidade/emoção). Validado convivendo
# com o braço (numpy 2.4.6). ATENÇÃO: NÃO instalar 'onnxruntime' puro — usar SÓ o
# 'onnxruntime-openvino' (provê o OpenVINO EP, ~3.5x no CPU); os dois conflitam.
mediapipe==0.10.35           # pose (corpo) + hands (mãos) + face mesh (expressão)
insightface==1.0.1           # detecção (RetinaFace/SCRFD) + ArcFace (identidade) + idade/gênero
onnxruntime-openvino==1.24.1 # runtime ONNX com OpenVINO EP (CPU/iGPU/NPU)
openvino==2026.2.1           # runtime OpenVINO (lista dispositivos)
hsemotion-onnx==0.3.1        # emoção rotulada (8 classes), ONNX
